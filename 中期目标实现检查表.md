# 银龄安心中期目标实现检查表

> 检查日期：2026-07-24
> 目标依据：当前项目代码、接口实现、页面连接和构建结果；本次未读取具体知识库内容。
> 检查范围：`src/`、根目录代码/配置、工作流调用封装和测试脚本。`[x]` 表示该行所述范围已实现；部分实现的目标已拆成“已完成部分”和“仍缺部分”两行，避免误报。
> 合并说明：本表已整合原“中期考核未完成及出入清单”的近期修正、未完成项和优先级建议，后续只维护本表。

| 编号 | 优先级 | 中期目标 / 验收点 | 已实现 | 未实现 | 实现证据或缺口 | 测试点 / 路径 |
|---|---|---|:---:|:---:|---|---|
| ACC-01 | P0 | 登录页包含用户名、手机号、验证码/测试密码 | [x] | [ ] | `src/app/login/page.tsx` 已有三项输入与前端校验 | 访问 `/login`，分别留空三项提交，检查对应提示 |
| ACC-02 | P0 | 用户协议与隐私说明分别勾选 | [x] | [ ] | 两个独立复选框，未同时勾选时阻止提交 | `/login` 只勾选一项后提交 |
| ACC-03 | P0 | “未注册首次登录自动注册”中期体验 | [x] | [ ] | `completeLocalLogin` 按用户 ID 创建浏览器本地账户；选择身份后保持登录态 | 新手机号登录→进入 `/role-select`→选择身份→刷新首页 |
| ACC-04 | P0 | 登录请求字段和后端格式包含用户名 | [x] | [ ] | `POST /api/auth/login` 接收 `username/phone/code/agreements`；`UserAccount` 含 `username` | 对接口提交合法/非法用户名，检查 200/400 |
| ACC-05 | P0 | 真实验证码或测试凭证校验 | [ ] | [x] | 当前接口只检查验证码非空，没有短信服务或受控测试密码 | 任意非空验证码仍能通过；公开部署前必须补齐 |
| ACC-06 | P0 | 服务端账户数据库与安全会话 | [ ] | [x] | 已有 `GET/PATCH /api/users/[userId]` 和进程内用户资料 Store，但登录会话仍在 `localStorage`，无数据库及 HttpOnly Cookie/Token | 清除浏览器存储后会话消失；重启服务后接口修改的资料恢复为模拟值 |
| ACC-07 | P0 | 登录后顶部显示用户名首字圆形头像 | [x] | [ ] | `AuthNav` 使用蓝色圆形头像；英文首字母转大写，中文取首字 | 以“王阿姨”和“alice”登录，检查显示“王”和“A” |
| ACC-08 | P0 | 安全退出并清除当前登录态 | [x] | [ ] | 账户中心调用 `clearAuthSession`，清除会话和角色后返回登录页 | `/account` 点击“退出登录”，再访问首页和 `/upload` |
| ACC-09 | P0 | 首页上传按钮未登录时拦截并提示 | [x] | [ ] | 未登录按钮显示红底白字“请先登录”并在2秒后消失；登录后才链接 `/upload` | 清空存储，在 `/` 点击“拍照或上传材料”，观察提示计时 |
| ACC-10 | P0 | 直接访问上传页也要求登录 | [x] | [ ] | `/upload` 在渲染上传控件前检查浏览器登录态 | 未登录直接访问 `/upload`，应只见“请先登录” |
| ACC-11 | P0 | 受保护页面统一客户端登录守卫 | [x] | [ ] | 根布局通过 `ProtectedRouteGuard` 统一保护上传、结果、家庭、历史、账户等非公开页面，未登录直接访问会跳转 `/login` | 未登录分别直接打开 `/family`、`/account`、`/medicine`、`/history` 等 URL |
| ACC-14 | P0 | 所有受保护页面统一服务端鉴权 | [ ] | [x] | 仍无基于 HttpOnly Cookie/Token 的统一服务端守卫；客户端检查不能作为真实安全边界 | 删除/篡改本地存储并直接请求业务 API，服务端当前无法统一拒绝 |
| ACC-15 | P0 | 客户端守卫按身份限制页面并开放老人端简报 | [x] | [ ] | `PATH_ROLE_REQUIREMENTS` 集中配置允许身份；`/family` 允许老人和家属访问，`/profile-setup` 只允许老人，不符合时跳转 `/forbidden` | 老人和家属分别访问 `/family`；家属访问 `/profile-setup` 应被拦截 |
| ACC-12 | P0 | 登录页不重复显示右上角“登录”入口 | [x] | [ ] | `AuthNav` 在 `/login` 返回空内容，其他未登录页面仍显示登录入口 | 访问 `/login` 和 `/` 对比右上角导航 |
| ACC-13 | P0 | 登录后未选身份返回首页应恢复未登录状态 | [x] | [ ] | 首页发现会话尚无角色时清除临时会话，并按未登录状态渲染，不再同时出现头像与“去登录” | `/login`→`/role-select`→点击左上角“银龄安心” |
| ROLE-01 | P0 | 首次身份选择只有老人、家属两项 | [x] | [ ] | `/role-select` 仅提供 `elder`、`family`，医生/药师未出现 | 首次登录后检查选项和路由 |
| ROLE-02 | P0 | 身份在刷新和再次登录后保持 | [x] | [ ] | 角色写入当前浏览器账户记录，重新登录同一账户可恢复 | 选择身份→退出→同手机号再次登录 |
| ROLE-03 | P0 | 服务端白名单校验并持久化身份 | [ ] | [x] | 当前身份选择只写浏览器存储，无 `/api/profile/role` 和数据库校验 | 在开发者工具篡改本地角色；服务端不会拒绝 |
| ROLE-04 | P0 | 身份选择后的页面分流 | [x] | [ ] | 老人确认身份后进入 `/profile-setup`；家属/照护者确认后直接进入 `/family-bind` | 新账户分别选择两种身份，核对跳转页面 |
| ROLE-05 | P0 | 老人首次补充出生年份和基础疾病 | [x] | [ ] | `/profile-setup` 收集出生年份及是否有基础疾病，经 `PATCH /api/users/[userId]` 保存后进入家庭绑定；`UserProfile` 已增加 `has_chronic_disease` | 老人选择身份→填写资料→进入 `/family-bind`→再到 `/account` 核对两项资料 |
| HOME-01 | P0 | 老人/家属身份化首页 | [x] | [ ] | 首页按角色展示不同上传文案、绑定、预警、历史和账户入口 | 分别以两种角色进入 `/` 对照入口 |
| HOME-02 | P0 | 家属先选择老人再代上传的中期 UI | [x] | [ ] | 家属首页点击“代老人上传材料”时，多老人场景先弹出选择框；进入 `/upload` 后仍可再次确认，未选择时不能分析 | 家属登录后点击首页代上传，选择老人并核对 `/upload?elder_id=...` 及下拉框 |
| HOME-03 | P0 | 老人选项来自绑定关系与代上传权限 | [x] | [ ] | 首页和上传页读取 `GET /api/family/relations`，只展示 `status=active` 且 `upload_for_elder=true` 的老人；当前关系来自进程内 Store | 关闭某老人代上传权限或解除绑定后，重新进入首页/上传页，该老人应消失 |
| HOME-04 | P0 | 老人选项使用真实数据库关系并由服务端鉴权 | [ ] | [x] | 当前关系接口仍是进程内 Map，且请求使用模拟家属 ID；未根据安全会话校验当前操作者 | 伪造 `family_user_id` 请求关系接口，正式实现应返回403 |
| BIND-01 | P0基础版 | 老人端生成6位绑定码 UI | [x] | [ ] | `/family-bind` 可显示6位模拟码及倒计时界面 | 老人身份进入 `/family-bind` 点击生成 |
| BIND-02 | P0基础版 | 家属输入6位测试码并选择关系，建立演示绑定 | [x] | [ ] | `/family-bind` 对测试码 `731508` 调用 `POST /api/family/relations`，接口直接建立 `active` 关系；其他6位码仍提示“功能持续开发中” | 家属身份输入测试码与非测试码，分别检查成功和开发中分支 |
| BIND-03 | P0基础版 | 绑定码有效期、撤销、一次性使用由后端校验 | [ ] | [x] | 当前绑定码来自 `MOCK_FAMILY_INVITE`，没有接口和持久化状态机 | 测试过期码、重复码、撤销码，当前无法真实判定 |
| BIND-04 | P0基础版 | 创建待确认关系并由老人确认/拒绝 | [ ] | [x] | 页面展示模拟关系，未创建 `pending` 关系，也没有确认/拒绝接口 | 两账户走完整绑定流程，刷新后状态不会真实保存 |
| BIND-05 | P0基础版 | 红色预警、家属简报、代上传等权限配置 UI | [x] | [ ] | 老人绑定页保留权限展示；家属账户中心可逐位老人打开独立权限弹窗，覆盖6项权限 | 家属进入 `/account`，分别修改两位老人权限 |
| BIND-06 | P0基础版 | 权限修改接口及多页面进程内同步 | [x] | [ ] | `PATCH /api/family/relations/[relationId]` 更新进程内关系 Store；账户、上传、预警和历史页读取同一关系接口 | 修改权限后重新进入相应页面，核对老人选项和“已开启/已关闭”状态 |
| BIND-11 | P0 | 权限数据库持久化及后端逐请求鉴权 | [ ] | [x] | 当前权限修改依赖进程内 Map，请求体中的家属 ID 尚未由安全会话确认；业务详情接口也未逐项校验权限 | 重启服务后权限会恢复；伪造家属 ID 或直接请求详情接口仍需返回403 |
| BIND-07 | P0 | 浏览器当前操作区分 `operator_user_id` 与 `subject_user_id` | [x] | [ ] | 上传前把当前账户记录为操作人、所选老人记录为材料所属人并写入本次 `sessionStorage` 上下文 | 家属上传后检查 `yinling_current_operation_context`，两字段应不同 |
| BIND-10 | P0 | 服务端校验并持久化操作人、材料所属人和关系 | [ ] | [x] | 当前上传/分析接口和数据库未接收、校验或保存上述归属字段 | 检查请求和数据库记录；伪造未绑定老人 ID 时服务端应返回 403 |
| BIND-08 | P1 | 中期解除绑定：二次确认、立即删除及全页同步 | [x] | [ ] | 家属端逐条解除；`DELETE /api/family/relations/[relationId]` 立即删除进程内关系并返回撤权状态；成功提示3秒后消失，账户中心重新读取接口后不再显示该老人 | 家属解绑一位老人→进入 `/account`、首页代上传、预警和历史页，均不应再出现该老人 |
| BIND-09 | P1 | 正式数据库持久化删除及全站服务端撤权 | [ ] | [x] | 当前后端关系仓库为进程内 Map，正式数据库和所有业务接口的逐请求鉴权仍未接入 | 重启服务、越权访问老人详情和多实例部署场景 |
| UP-01 | P0 | JPG/PNG 单图选择、预览、删除、取消 | [x] | [ ] | 上传页检查图片 MIME、10MB 上限并生成预览 | `/upload` 选图、删图、取消并重新选择 |
| UP-02 | P0 | 拍照/相册/文件入口 | [x] | [ ] | 上传页分别提供“拍照”和“相册或文件”两个入口，移动端拍照入口带后置相机提示 | 手机浏览器访问 `/upload` 分别选择拍照和相册 |
| UP-03 | P0 | 多图选择、排序并全部进入后端分析 | [ ] | [x] | 当前两个文件输入均未启用 `multiple`，分析代码也明确只处理 `files[0]` | 一次选择两张图或连续追加图片，检查是否形成同一材料组并全部进入分析 |
| UP-04 | P0 | 图片裁剪与方向调整 | [x] | [ ] | 预览图支持每次旋转90°；自动裁切按四角背景色、颜色差和局部边缘强度估算文件/药盒主体，保留安全边距后再上传，不再固定裁四周 | 分别上传横放文件、竖放药盒、复杂背景和低对比度图片，检查裁切与失败回退 |
| UP-05 | P0 | 上传前人工敏感信息确认 | [x] | [ ] | 隐私确认栏位于旋转和自动裁切之后；未检查调整后图片并勾选时不能开始分析，重新选图或调整图片会重置勾选 | 调整图片后检查勾选是否重置；不勾选时检查分析按钮状态 |
| UP-06 | P0 | OCR后自动识别并提示敏感字段 | [x] | [ ] | 客户端按身份证、银行卡、手机号、验证码、地址规则复核 OCR；命中后暂停并提供重新选择、明确确认继续、取消上传 | 上传含模拟敏感号码的测试图，检查三种操作分支 |
| UP-09 | P0 | 在原图精确高亮敏感区域 | [ ] | [x] | 当前分析接口只返回 OCR 文本，没有文字框坐标；页面只能提示敏感类别和原文，精确遮挡需 OCR 接口返回坐标 | 检查 `/api/analyze` 响应是否含每段文字的 bounding box |
| UP-07 | P0 | 真实上传、OCR与材料分类接口连接 | [x] | [ ] | `/api/upload`、`/api/analyze` 已接入，上传页按步骤调用 | 使用有效环境配置上传清晰测试图 |
| UP-08 | P0 | 上传/识别过程状态和失败提示 | [x] | [ ] | 页面有 uploading、ocr、understanding、error 等状态 | 断网、错误格式、超限文件、接口失败分别测试 |
| CLS-01 | P0 | 分类结果、置信度和候选类型展示 | [x] | [ ] | `/confirm` 展示工作流分类结果并提供类型选项 | 上传后进入 `/confirm` 检查低/中/高置信度 |
| CLS-02 | P0 | 用户确认分类后进入对应结果页 | [x] | [ ] | 按选中类型路由到 `/medicine` 或 `/fraud` | 分别确认药品类和防骗类材料 |
| CLS-03 | P0 | 手动修正分类并回写当前会话材料 | [x] | [ ] | 确认时更新 `material_type/type_label/route`，保存当前材料，并记录 `user_corrected_type` 会话审计信息 | 改选类型后进入结果页，检查读取的材料类型和 sessionStorage |
| CLS-04 | P0 | 分类修正提交分析接口并持久化 | [ ] | [x] | 尚无分类纠正提交接口或数据库字段，刷新/跨设备无法形成可追踪反馈 | 检查网络请求和数据库中的 `user_corrected_type` |
| MED-01 | P0 | 药品接口真实结果连接到结构化 UI | [x] | [ ] | 页面直接读取接口返回的 `Medicine`；显示适配器现可解析 `CitationField.display_value/material_value/catalog_value`，修复 OCR 有药名但 UI 显示“未识别” | 上传药品材料→`/confirm`→`/medicine`，对照接口药名与页面药名 |
| MED-02 | P0 | 大字版药名、规格、批准文号、生产企业 | [x] | [ ] | 结果页按字段卡片展示，缺失字段显示“未识别” | 用药盒测试图检查四项信息 |
| MED-03 | P0 | 用量、频次、时间、适应症结构化展示 | [x] | [ ] | 结果页已有“用法用量”和“适应症”卡片 | 用处方/说明书测试图检查字段 |
| MED-04 | P0 | 适用人群/用途、禁忌、重要提醒、不良反应、储存条件固定保留 | [x] | [ ] | 最终结果页固定展示全部字段；人工或 OCR 识别为“无”时原样显示，无相关信息时统一显示“未识别到相关信息，请仔细核对药品说明” | 分别使用字段为“无”和字段缺失的两份结果，检查五项字段均不消失 |
| MED-05 | P0 | 医生用药说明文本框 | [x] | [ ] | OCR 修正结果保存后才展示500字医生说明文本框、保存状态和重新整理按钮 | `/medicine` 先保存 OCR 修正，再输入“每天早饭后服用一片” |
| MED-06 | P0 | 医生说明传入工作流且只更新明确涉及字段 | [x] | [ ] | `user_dose_note` 提交至 `/api/medicine` 和 `callWF2`；页面按关键词合并，只替换医生说明明确涉及的字段，其余人工修正 OCR 值和来源保持不变 | 先手工修改药名，再仅输入服药时间；重新整理后药名和原来源不得变化 |
| MED-07 | P0 | 首屏以整理卡片为主，OCR原文默认折叠 | [x] | [ ] | 原 OCR 大框改为默认折叠的 `<details>` | `/medicine` 首屏检查，手动展开原文 |
| MED-08 | P0 | 药品批准文号/真实性核验提示 | [x] | [ ] | 展示 `authenticity_flags` 的等级、风险信号和核验建议 | 上传批准文号不清或不一致样本 |
| MED-09 | P0 | 每个关键字段可追溯到具体材料/知识来源 | [x] | [ ] | 新增“查看逐项信息来源”，按药名、剂量、禁忌等字段展示上传材料或知识库/备案资料及位置 | `/medicine` 展开逐项来源，对照 WF-2 返回的 `sources` |
| MED-10 | P0 | 红色药品风险创建预警并检查家属授权 | [ ] | [x] | 结果页只有跳转入口，未创建 `alert_event` 或通知记录 | 构造红色真实性风险并观察网络请求 |
| MED-11 | P0 | OCR 结果紧凑修正、保存并进入只读结果 | [x] | [ ] | 保存前显示单行高度的药名、批准文号、用法、提醒等修正框及“如果OCR识别有误……”提示；保存后通过 `POST /api/medicine/records` 写入记录并切换只读版 | 修改药名或批准文号→保存→检查输入框消失、历史页出现新记录 |
| RISK-01 | P0 | 防骗结果调用真实 WF-3 | [x] | [ ] | `/api/fraud` 调用 `callWF3`，结果页读取真实响应 | 上传推销聊天/宣传材料进入 `/fraud` |
| RISK-02 | P0 | 绿/黄/红三级风险图标与文字 | [x] | [ ] | `RiskBadge` 配合文字和颜色展示，不仅靠颜色 | 分别使用三档测试样本 |
| RISK-03 | P0 | 风险证据、原因、立即停止动作和核验建议 | [x] | [ ] | 防骗页按工作流风险信号结构展示 | 红色样本检查证据和停止动作置顶 |
| RISK-04 | P0 | 红色风险先落预警记录再通知 | [ ] | [x] | 没有 alert API/数据库；`/alerts` 读取模拟对象 | 红色分析后检查是否新增可追踪预警 |
| RISK-05 | P1 | 无绑定/无授权时仅老人端提示 | [ ] | [x] | 当前没有真实关系与权限判断分支 | 红色样本在无家属、权限关闭两种状态测试 |
| FAM-01 | P1 | 老人和家属均可查看结构化家属简报 | [x] | [ ] | `/family` 允许两种身份访问，展示材料、产品、付款、风险、证据和行动项；未登录仍由统一守卫拦截 | 老人、家属分别从结果页进入 `/family`；未登录直接访问应跳转 `/login` |
| FAM-02 | P1 | 家属简报使用当前材料和分析结果动态生成 | [x] | [ ] | 已移除页面对 `MOCK_FAMILY_BRIEF` 的依赖；`buildFamilyBriefFromAnalysis` 从当前 `Material`、已保存 `Medicine` 或 `Risk` 提取摘要、药名/对象、风险证据和行动建议，无分析上下文时不展示假模板 | 连续分析两份不同材料并分别生成简报，核对药名、OCR证据和风险建议随材料变化 |
| FAM-03 | P1 | 家属简报导出长图及规范文件名 | [x] | [ ] | “导出图片”使用 Canvas 生成含账户ID、账户名、完整手机号、材料、财务安全、证据和行动建议的 PNG；文件名为“账号名_家属简报_导出时间_来源端” | `/family` 点击“导出图片”，检查 PNG 内容与文件名中的老人端/家属端/照护者端 |
| FAM-04 | P1 | 无材料图片时不产生空 `img src` | [x] | [ ] | 仅在 `material_image` 非空时渲染图片区域，已消除 React 空 `src` 控制台错误 | `/family` 使用无图片数据并检查控制台 |
| FAM-05 | P1 | 生成三天有效的公开家属简报链接 | [x] | [ ] | `POST /api/family/share` 保存当前动态简报并生成3天到期链接；公开 `/share/family/[token]` 无需登录，创建后复制到剪贴板 | 从 `/family` 点击转发→无痕窗口打开链接→核对内容；模拟到期后应返回失效状态 |
| FAM-06 | P0 | 简报生成、导出和分享前校验当前用户、关系与授权 | [ ] | [x] | `/family` 只检查浏览器角色；分享接口接受任意 `brief`，未校验登录会话、材料归属或 `view_family_report` 权限 | 伪造简报请求、关闭权限后直接访问 `/family` 或调用分享接口，正式实现应返回403 |
| FAM-07 | P0 | 分享最小披露、撤销和访问审计 | [ ] | [x] | 公开链接仅有3天过期机制，无主动撤销、访问日志和二次验证；图片/PDF导出还包含账户ID与完整手机号 | 分享后立即撤销并再次访问；检查导出内容默认不暴露完整手机号 |
| ALERT-01 | P1基础版 | 老人/家属预警中心及多老人切换 | [x] | [ ] | `/alerts` 按角色展示两套视图；家属页顶部读取获红色预警权限的绑定老人下拉框，切换后刷新对应预警、证据和通知状态 | 家属进入 `/alerts`，在王阿姨和张叔叔之间切换并核对内容变化 |
| ALERT-02 | P1基础版 | 站内通知“我已查看”浏览器状态变更 | [x] | [ ] | 家属点击后状态变为“已确认”并写入 localStorage，同一浏览器刷新和老人视图可读取 | 家属点击“我已查看”→刷新→切老人身份查看状态 |
| ALERT-04 | P1基础版 | 通知状态服务端持久化与跨账户同步 | [ ] | [x] | 当前预警和通知仍为模拟数据，本地状态不能跨设备同步，需通知接口和数据库 | 家属在设备A确认后，老人账户在设备B应看到已确认 |
| ALERT-03 | P1基础版 | 通知失败、无接收人和重试 | [ ] | [x] | 只有模拟文案，没有发送记录、失败接口和重试动作 | 模拟通知失败，检查预警是否保留并可重试 |
| HIST-01 | P1 | 历史列表、多人切换、筛选、三种状态和删除交互 | [x] | [ ] | `/history` 有筛选、批量选择、二次删除与5秒撤销；家属可切换3位获授权老人；模拟记录覆盖“分析中/待确认/已确认”，只有待确认显示“确认记录” | 家属切换3位老人；分别检查三种状态；确认待确认记录；测试单条、批量和全部删除撤销 |
| HIST-02 | P1 | 所有分析记录的授权读取和永久删除 | [ ] | [x] | 药品保存已有记录接口和可选数据库存储，但防骗记录尚未写入；历史读取未按安全会话逐请求鉴权，删除目前只改前端状态，未永久删除服务端记录 | 保存药品与防骗结果后刷新；直接请求他人记录；删除后重启服务检查记录是否恢复 |
| HIST-03 | P1 | Markdown 排版的详细 PDF/批量导出 | [x] | [ ] | 导出按钮先组织 Markdown 分节，再生成无 UI 打印页供“另存为 PDF”；包含账户信息、OCR原文、详细结论、建议和来源，多条记录各自另起一页，按钮文案恢复为“导出” | 单条导出检查详情；多选导出检查每条记录从新页开始及文件名“账号名_历史记录合集_时间_来源端” |
| HIST-04 | P1 | 药品分析记录接口及可选数据库保存 | [x] | [ ] | `GET/POST /api/medicine/records` 保存材料、结构化药品、医生说明和所属老人；配置 `PGDATABASE_URL` 时写数据库，未配置时使用服务端内存 | 保存药品结果→刷新 `/history`；配置数据库后重启服务检查药品记录仍存在 |
| HIST-05 | P1 | 按记录ID重新加载真实历史详情 | [ ] | [x] | “查看详情”只跳转 `/medicine` 或 `/fraud`，没有携带记录ID，也不会把所选历史记录恢复为当前材料 | 从历史页打开两条不同记录，详情页当前可能仍显示最近一次会话材料 |
| PRIV-01 | P0 | 隐私说明页和上传最小化提醒 | [x] | [ ] | `/privacy` 说明处理范围、保存、分享和 AI 边界；首页/上传有提醒 | 访问 `/privacy` 并核对上传前提示 |
| PRIV-02 | P0 | 协议文字可打开对应用户协议与隐私说明 | [x] | [ ] | 登录页协议名称带下划线，可在页面内打开适老可滚动弹窗 | `/login` 分别点击《用户协议》和《隐私说明》，再点击右上角关闭 |
| PRIV-03 | P0 | 用户协议与隐私说明正式合规定稿 | [ ] | [x] | 当前内容为中期草稿，仍需结合正式存储、第三方服务和法律审核定稿 | 公开部署前完成法务与数据处理流程复核 |
| PRIV-04 | P0 | 查阅、更正、导出和删除个人数据的真实请求流程 | [ ] | [x] | 页面只有说明文字和前端删除交互，没有账户级数据导出、删除申请、处理记录和完成回执 | 提交删除请求后核对数据库、对象存储、分享链接和审计记录 |
| ERROR-01 | P0 | 上传、分析、无材料等核心错误状态 | [x] | [ ] | 上传、药品、防骗页有错误提示，另有 `/error` 状态页 | 断网、清空会话材料后直接访问结果页 |
| ERROR-02 | P0 | 无权限专用页面 | [x] | [ ] | `/forbidden` 在身份不符合页面配置时展示明确提示及首页、家庭绑定入口；老人访问简报属于允许场景 | 家属身份直接访问仅老人可用的 `/profile-setup` |
| ERROR-03 | P0 | 服务端无权限拒绝 | [ ] | [x] | 家庭权限 API 尚未按登录账户和关系返回 403，客户端守卫不能替代服务端鉴权 | 直接调用未授权业务 API，服务端应拒绝且不返回敏感字段 |
| UI-01 | 全局 | 适老字号、主按钮尺寸、高对比、响应式单列 | [x] | [ ] | 页面普遍使用 `text-base` 以上、约56px主按钮和 `sm:` 响应式布局 | 360px、768px、桌面宽度检查横向滚动和点击区 |
| UI-02 | 全局 | 风险不能只靠颜色表达 | [x] | [ ] | 风险徽章同时包含等级文字/图标，警告卡有明确动作文本 | 关闭颜色感知或灰度截图检查 |
| UI-03 | 全局 | 键盘、读屏、焦点、对比度和缩放无障碍验收 | [ ] | [x] | 已有部分标签、`role` 和响应式样式，但未见自动化无障碍测试或完整人工验收记录 | 仅键盘完成主链路；200%缩放、读屏和对比度检查 |
| ACCT-01 | P1 | 账户中心展示账户ID、资料、身份、家庭权限和退出 | [x] | [ ] | `/account` 展示账户ID、用户名、脱敏手机号和角色；老人额外显示出生年份和基础疾病；家庭关系按接口列出 | 两种身份分别访问 `/account`，核对账户ID、资料与家庭成员 |
| ACCT-02 | P1 | 保存期限选择在浏览器中记忆 | [x] | [ ] | 账户中心和历史页按当前用户保存同一浏览器偏好，刷新后保持选择，并明确提示尚未执行真实删除 | 切换保存期限后刷新 `/account` 和 `/history` |
| ACCT-03 | P1 | 保存期限驱动服务端到期删除 | [ ] | [x] | 需要真实历史记录数据库、定时删除任务和删除审计；当前仅保存偏好 | 等待超过期限或运行删除任务，验证记录和原图从服务端清除 |
| ACCT-04 | P1 | 用户名和手机号支持修改，手机号修改需验证码 | [x] | [ ] | 两项资料旁有灰色笔形按钮；手机号弹窗要求同时输入新手机号和验证码，`PATCH /api/users/[userId]` 验证后更新服务端 Store 与当前浏览器登录信息 | 用户名直接修改；手机号分别输入错误验证码和测试验证码123456，检查拒绝与成功分支 |
| ACCT-05 | P1 | 家属账户展示全部老人并修改权限 | [x] | [ ] | 家属账户中心按关系接口展示3位模拟老人；每张信息卡右侧有“修改权限”弹窗并调用关系 PATCH 接口 | 家属进入 `/account`，分别打开3位老人权限弹窗并保存 |
| ACCT-06 | P1 | 老人端只查看家庭成员和权限 | [x] | [ ] | 老人账户及首页文案改为“查看家庭成员和权限”；账户页展示两名子女/亲属和一名照护者，并提示解绑或修改权限需联系子女在家属端处理 | 老人进入首页和 `/account` 核对入口文案、三名成员及提示 |
| ACCT-07 | P0 | 账户、家庭关系和老人选择全部使用当前安全会话身份 | [ ] | [x] | 首页、账户、上传、历史和预警等页面仍多处使用 `MOCK_USER_PROFILE_FAMILY/ELDER` 固定ID查询数据 | 使用两个真实测试账号登录，二者应只看到各自资料和关系 |
| EXT-01 | 中期后 | 问医生清单自定义问题和打印 | [x] | [ ] | `/doctor-questions` 可勾选、自定义并调用打印 | 添加问题、勾选、点击打印 |
| EXT-02 | 中期后 | 问医生清单根据当前材料生成/导出图片 | [ ] | [x] | 清单来自 `MOCK_DOCTOR_QUESTIONS`，导出和重新生成仅提示开发中 | 从药品结果进入并比较不同材料 |
| EXT-03 | 中期后 | 对照、证据、反馈页面基础 UI | [x] | [ ] | `/compare`、`/evidence`、`/feedback` 页面存在 | 逐页检查入口、返回和空状态 |
| EXT-04 | 中期后 | 对照、证据、反馈真实数据与提交接口 | [ ] | [x] | 主要读取模拟数据，反馈无提交接口 | 提交反馈或分析不同材料后对照内容 |
| EXT-05 | 中期后 | 反馈页补充图片控件符合页面要求 | [x] | [ ] | 文件状态显示在圆角“选择文件”按钮上方 | `/feedback` 选择和取消补充图片 |
| EXT-06 | 中期后 | 多药材料拆分、逐药确认和相互作用提醒 | [ ] | [x] | 类型中已有 `multiple_medicines`，但 `getRouteByType` 未给出结果路由，也没有多药拆分页面或记录模型 | 分类为“多种药品”后逐药确认，并对重复成分/相互作用给出谨慎提示 |
| EXT-07 | 中期后 | 证据包真实汇总与导出 | [ ] | [x] | `/evidence` 使用模拟数据，“导出证据包”仅弹出开发中提示 | 从当前分析生成时间线、证据清单并导出可核验文件 |
| ENV-01 | 交付 | 本地密钥文件不进入 Git | [x] | [ ] | `.gitignore` 明确包含 `.env.local`，当前文件存在但未被 Git 跟踪 | 运行 `git check-ignore -v .env.local` 和 `git ls-files .env.local` |
| QA-01 | 交付 | TypeScript、ESLint、生产构建通过 | [x] | [ ] | 2026-07-24 已重新执行 `tsc --noEmit`、全量 ESLint quiet 检查和 Next production build；34个路由生成成功 | 改动后重复运行三项命令 |
| QA-02 | 交付 | 自动化端到端测试覆盖中期主链路 | [ ] | [x] | 仓库未发现覆盖登录→绑定→分析→预警的自动化用例 | 建立至少登录、分类、药品、防骗、权限五组 E2E |
| QA-03 | 交付 | API、权限和数据仓库的单元/集成测试 | [ ] | [x] | 当前只有图片样本和一个手工 WF-1 调用脚本，未见断言式自动测试 | 覆盖登录校验、越权、绑定状态机、记录过滤、分享过期和工作流错误映射 |
| SEC-01 | P0 | API限流、重放防护、上传安全扫描和统一输入校验 | [ ] | [x] | 现有接口做了部分格式/大小校验，但没有统一鉴权、限流、重放防护、恶意文件扫描和 CSRF 保护 | 高频登录/上传/分享请求、重复提交、伪造跨站请求与异常文件测试 |
| SEC-02 | P0 | 敏感日志最小化与可审计的安全日志 | [ ] | [x] | 工作流调用仍会输出部分原始结果，上传接口记录文件信息；尚无字段脱敏、日志分级、留存策略和安全事件审计 | 检查日志不得出现完整手机号、验证码、OCR全文、绑定码和访问令牌 |

## 当前结论

- 已可演示的主链路：本地登录/自动注册 → 身份选择分流 → 老人资料补充或家属绑定 → 多老人代上传选择 → 上传真实材料 → 分类确认 → 药品结构化说明或防骗结果。
- 本次已补齐：药品 OCR 紧凑修正与保存、医生说明选择性更新、重要提示缺失兜底、老人端家属简报、动态简报数据、三天公开分享、规范长图文件名、历史三状态及确认按钮、详细 PDF 分页导出，以及3位老人、4位家属/照护者和9条记录的本地样例。
- 既有修正仍包括：客户端登录与身份守卫、老人出生年份/基础疾病、账户ID与资料修改、图片旋转和自适应主体裁切、OCR敏感字段二次确认、分类修正会话回写、药品逐项来源、家属多老人选择与权限、解绑同步、预警本地确认、保存期限浏览器记忆和空图片问题。
- 中期最大缺口：真实安全会话、绑定申请/老人确认状态机、家庭关系数据库、服务端逐请求权限校验、红色预警与站内通知落库仍未完成；历史方面只有药品记录接口，防骗记录、服务端永久删除和完整授权读取仍缺。
- 代码级安全结论：当前版本适合受控演示，不宜按生产系统对外开放；所有业务 API、公开分享和历史读取必须先补齐服务端身份与授权边界。

## 当前优先级建议

1. 首先把现有用户资料与家庭关系 Store 替换为数据库，并接入真实账户会话和统一服务端权限校验。
2. 再完成绑定申请→老人确认→授权→解除的完整状态机，以及操作人/材料所属老人字段落库。
3. 然后完成红色预警落库、站内通知状态，并把现有药品记录接口扩展到防骗记录、服务端授权读取和永久删除。
4. 最后补多图服务端分析、敏感文字框坐标、反馈提交和端到端测试。

## 全量功能完成路线表

> 分工口径：成员A＝产品/UI/前端；成员B＝知识与内容规则；成员C＝后端/工作流/数据/安全；成员D＝测试/部署/文档。`[x]` 仅表示该工作包已完整实现；“部分完成”仍保持未勾选。

| 顺序 | 优先级 | 功能工作包 | 对应检查项 | 完成 | 当前状态/验收边界 | 主责 | 配合 |
|---:|---|---|---|:---:|---|---|---|
| 1 | P0 | 真实登录、验证码、安全会话、退出和统一服务端守卫 | ACC-01～15、SEC-01 | [ ] | 页面与客户端守卫已完成；真实验证码、HttpOnly会话、CSRF/限流和服务端守卫未完成 | C | A、D |
| 2 | P0 | 账户资料、身份和老人健康资料数据库持久化 | ROLE-01～05、ACCT-01～07 | [ ] | UI与进程内资料修改已完成；身份仍在浏览器，多个页面仍使用固定模拟ID | C | A、D |
| 3 | P0 | 家庭绑定码、待确认、授权、拒绝、过期、撤销状态机 | BIND-01～04 | [ ] | 测试码可直接建立演示绑定；真实一次性码和老人确认状态机未完成 | C | A、D |
| 4 | P0 | 家庭关系与六项权限数据库、逐请求鉴权和全站撤权 | BIND-05～11、HOME-03～04 | [ ] | 权限UI和进程内PATCH/DELETE已完成；数据库、老人授权语义和服务端逐请求校验未完成 | C | A、D |
| 5 | P0 | 操作人/材料所属老人/关系链路服务端校验与落库 | BIND-07、BIND-10 | [ ] | 浏览器会话上下文已区分两类ID；接口未验证或完整保存 | C | A、D |
| 6 | P0 | 身份化首页、登录拦截、家属选老人代上传 | HOME-01～03、ACC-09～11 | [x] | 中期UI链路可演示；生产验收仍依赖顺序1～5 | A | C、D |
| 7 | P0 | 单图选择、拍照、预览、删除、旋转、自适应裁切与隐私确认 | UP-01、UP-02、UP-04、UP-05 | [x] | 代码已实现；需用真实手机和复杂背景样本回归 | A | C、D |
| 8 | P1 | 多图选择、排序、材料组上传和完整分析 | UP-03 | [ ] | 文件输入未开放多选，后端只处理首张图 | A | C、D |
| 9 | P0 | 对象存储、OCR、分类工作流、进度与错误映射 | UP-07、UP-08、CLS-01～02 | [x] | WF-1与对象存储接口已连接；生产稳定性依赖环境配置与回归 | C | A、D |
| 10 | P0 | OCR敏感信息检测、坐标高亮、遮挡和二次确认 | UP-06、UP-09、PRIV-01 | [ ] | 文字规则提示已完成；坐标、高亮和自动遮挡未完成 | A | C、D |
| 11 | P1 | 分类人工修正、服务端回写、审计与训练反馈 | CLS-03～04 | [ ] | 当前会话可修正；接口和数据库反馈闭环未完成 | C | A、B、D |
| 12 | P0 | 药品结构化解读、OCR修正、保存、医生说明和来源展示 | MED-01～09、MED-11 | [x] | WF-2、结构化UI、修正保存和药品记录接口已完成 | A | B、C、D |
| 13 | P1 | 药品权威核验、来源版本、缺失兜底和红色真实性预警 | MED-08～10 | [ ] | 页面核验提示已完成；权威核验闭环和预警落库未完成 | B | C、A、D |
| 14 | P1 | 多药拆分、逐药确认、重复成分/相互作用提示 | EXT-06 | [ ] | 只有类型定义，没有可用结果链路 | B | A、C、D |
| 15 | P0 | 防骗分析、三级风险、原文证据、停止动作和核验建议 | RISK-01～03 | [x] | WF-3和结果UI已完成 | B | C、A、D |
| 16 | P0 | 红色预警事件、授权收件人、站内通知、已读/确认和失败重试 | RISK-04～05、ALERT-01～04 | [ ] | 预警中心UI和浏览器确认状态已完成；事件、通知和重试均未落库 | C | A、B、D |
| 17 | P1 | 动态家属简报、老人/家属查看、长图与打印 | FAM-01～04 | [x] | 当前材料可动态生成并导出；生产验收仍依赖真实授权 | A | B、C、D |
| 18 | P0 | 家属简报分享授权、最小披露、三天到期、撤销和访问审计 | FAM-05～07 | [ ] | 三天公开链接已完成；鉴权、撤销、审计和导出脱敏未完成 | C | A、D |
| 19 | P1 | 药品/防骗历史统一落库、授权读取、筛选、详情恢复和永久删除 | HIST-01～05 | [ ] | 药品记录与前端筛选/导出已完成；防骗、详情恢复、授权读取和删除未完成 | C | A、D |
| 20 | P1 | 保存期限执行、对象存储清理和删除审计 | ACCT-02～03、PRIV-04 | [ ] | 只有浏览器偏好，没有服务端到期任务 | C | A、D |
| 21 | P2 | 问医生清单按材料生成、自定义、打印与图片导出 | EXT-01～02 | [ ] | 自定义和打印已完成；动态生成、重生成和图片导出未完成 | A | B、C、D |
| 22 | P2 | 药品/保健品对照使用当前分析数据并合并重复路由 | EXT-03～04 | [ ] | 两个对照页面均使用模拟数据，尚未形成真实记录选择和对照 | A | B、C、D |
| 23 | P2 | 证据时间线、材料清单和证据包导出 | EXT-03、EXT-07 | [ ] | 基础UI已完成；真实数据和导出未完成 | A | B、C、D |
| 24 | P1 | 反馈关联真实记录、图片上传、提交、处理状态和人工复核 | EXT-03～05 | [ ] | 表单与多图预览已完成；提交接口、存储和复核闭环未完成 | C | A、B、D |
| 25 | P0 | 协议/隐私定稿、同意记录、数据权利和公开分享合规 | PRIV-02～04、FAM-06～07 | [ ] | 草稿弹窗已完成；正式文本、同意证据和数据权利流程未完成 | A | C、D |
| 26 | P1 | 适老响应式、风险多通道表达和完整无障碍验收 | UI-01～03 | [ ] | 适老样式和风险文字/图标已完成；键盘、读屏、缩放等系统验收未完成 | A | D |
| 27 | P0 | 单元、接口、权限、工作流错误和端到端自动化测试 | QA-02～03 | [ ] | 当前仅有样本与手工调用脚本 | D | A、B、C |
| 28 | P0 | 生产部署、环境校验、数据库迁移、监控告警、日志脱敏和回滚 | ENV-01、SEC-02、QA-01 | [ ] | 密钥忽略、类型检查、Lint和生产构建已通过；生产运维闭环未完成 | D | C |
