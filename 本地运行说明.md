
# 本地运行说明

本文档用于说明如何在本地启动项目并测试完整流程。

## 1. 环境要求

- Node.js >= 20
- pnpm >= 9

检查版本：

```bash
node -v
pnpm -v
```

---

## 2. 安装依赖

克隆项目后进入目录：

```
cd EL_yinling-anxin
```

安装依赖：

```bash
pnpm install
```

---

## 3. 配置环境变量

复制环境变量模板：

```bash
cp .env.example .env.local
```

然后根据实际环境填写 `.env.local`。

主要配置：

```env
COZE_TOKEN=
COZE_TOKEN_WF2=
COZE_TOKEN_WF3=
COZE_BUCKET_ENDPOINT_URL=
COZE_BUCKET_NAME=
COZE_WORKLOAD_IDENTITY_API_KEY=
```

其中：

* `COZE_TOKEN`、`COZE_TOKEN_WF2`、`COZE_TOKEN_WF3`：分别用于调用三个工作流；
* `COZE_BUCKET_*`：用于图片上传后的对象存储；
* `COZE_WORKLOAD_IDENTITY_*`：用于本地访问 Coze 沙箱对象存储。

在 Coze Coding 云端环境中，对象存储相关变量会自动注入；本地运行需要手动配置。

---

## 4. 启动开发服务器

执行：

```bash
pnpm dev
```

启动成功后访问：

```
http://localhost:5000
```



## 5. 功能测试

进入上传页面：

1. 上传任意药品图片（如药盒、说明书等）；
2. 系统自动完成：

   * 图片上传；
   * OCR 文字识别；
   * 材料类型判断；
   * 风险分析；
   * 药品真实性风险初筛；
3. 根据分析结果跳转至对应页面。

支持测试类型：

* 正规药品包装；
* 药品说明书；
* 保健品宣传材料；
* 疑似诈骗宣传页。

---

## 6. 常见问题

### 上传失败：对象存储未配置

检查：

* `.env.local` 是否存在；
* `COZE_BUCKET_ENDPOINT_URL`
* `COZE_BUCKET_NAME`
* `COZE_WORKLOAD_IDENTITY_API_KEY`

是否填写正确。

### WF-1 返回 401

检查：

* `COZE_TOKEN` 是否有效；
* 修改 `.env.local` 后是否重新启动开发服务器。

